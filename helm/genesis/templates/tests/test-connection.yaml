apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "genesis.fullname" . }}-test-connection"
  labels:
    {{- include "genesis.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "genesis.fullname" . }}:{{ .Values.service.apiPort }}']
  restartPolicy: Never