# Core dependencies for Project GENESIS
# Python 3.11.8 required

# Exchange interaction
ccxt==4.4.0  # Security updates and bug fixes

# Terminal UI
rich==13.7.0
textual==0.47.1

# Async operations
aiohttp==3.10.11  # Security patches for CVE-2024-42367
websockets==13.1  # Security and stability updates
ntplib==0.4.0

# Configuration and validation
pydantic==2.5.3
pydantic-settings==2.1.0

# Structured logging
structlog==24.1.0

# Security and secrets management
hvac==2.1.0  # HashiCorp Vault client
boto3==1.34.0  # AWS Secrets Manager
cryptography==41.0.7  # Encryption support

# Database migrations
alembic==1.13.1
SQLAlchemy==2.0.25
asyncpg==0.29.0  # PostgreSQL async driver

# Backup and scheduling
apscheduler==3.10.4  # Automated backup scheduling
deepdiff==6.7.1  # Configuration comparison

# Data analysis
pandas==2.2.3  # Security and performance updates
numpy==1.26.4  # Security patches

# Process management (for production)
supervisor==4.2.5

# System monitoring
psutil==5.9.8
prometheus-client==0.20.0  # Prometheus metrics
fastapi==0.111.0  # For Prometheus endpoint
uvicorn==0.30.1  # ASGI server for FastAPI
slowapi==0.1.9  # Rate limiting for FastAPI

# Distributed tracing
opentelemetry-api==1.24.0  # OpenTelemetry API
opentelemetry-sdk==1.24.0  # OpenTelemetry SDK
opentelemetry-exporter-otlp==1.24.0  # OTLP exporter
opentelemetry-instrumentation-aiohttp==0.45b0  # aiohttp instrumentation
opentelemetry-instrumentation-fastapi==0.45b0  # FastAPI instrumentation
opentelemetry-instrumentation-sqlalchemy==0.45b0  # SQLAlchemy instrumentation

# Backup and recovery
boto3==1.35.0  # AWS SDK for S3-compatible storage
apscheduler==3.10.4  # Scheduled backup tasks

# Two-Factor Authentication
pyotp==2.9.0  # TOTP generation and verification
qrcode[pil]==7.4.2  # QR code generation for 2FA setup

# JWT Token Management
PyJWT[crypto]==2.8.0  # JWT token generation and verification with cryptographic support
redis==5.0.1  # Redis client for session storage

# Password Security
bcrypt==4.2.0  # Secure password hashing with salt

# DR and Scheduling
croniter==2.0.1  # Cron expression parsing for scheduled DR drills